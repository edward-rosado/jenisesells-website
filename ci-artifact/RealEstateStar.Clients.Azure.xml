<?xml version="1.0"?>
<doc>
    <assembly>
        <name>RealEstateStar.Clients.Azure</name>
    </assembly>
    <members>
        <member name="T:RealEstateStar.Clients.Azure.AzureBlobDocumentStore">
            <summary>
            Azure Blob Storage-backed document store for the Platform tier.
            Blob path convention: {folder}/{fileName} maps to blob names under the container.
            The container is created on first write if it does not exist.
            </summary>
        </member>
        <member name="M:RealEstateStar.Clients.Azure.TableStorageIdempotencyStore.ParseKey(System.String)">
            <summary>
            Parses the key into PartitionKey + RowKey.
            Pattern: {pipeline}:{instanceId}:{step} → PK="{pipeline}:{instanceId}", RK="{step}"
            Fallback (fewer than 3 segments): PK=full key, RK="default"
            </summary>
        </member>
    </members>
</doc>
